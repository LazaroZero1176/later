# Xcode derived data & local build artifacts
xcode/build/
xcode/.build/
xcode/DerivedData*/
xcode/build-dmg-exit.txt
xcode/build-dmg-out.txt
*.xcuserstate
xcuserdata/

# Build artifacts (DMGs ship via GitHub Releases, not the repo)
Later-*.dmg

# macOS
.DS_Store
